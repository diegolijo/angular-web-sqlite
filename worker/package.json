{
  "name": "@cocodintech/sqlite-client",
  "version": "1.0.0",
  "description": "SQLite Client is a wrapper for Sqlite on Wasm that uses the Origin Private File System to persist the sqlite database file.",
  "scripts": {
    "delete": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "bundle": "rollup -c rollup.config.mjs --compact"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "^3.44.0-build2"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^24.0.1",
    "@rollup/plugin-json": "^6.0.0",
    "@rollup/plugin-node-resolve": "^15.0.1",
    "rollup": "^3.16.0",
    "rollup-plugin-cleanup": "^3.2.1",
    "rollup-plugin-ignore": "^1.0.10",
    "rollup-plugin-typescript2": "^0.34.1",
    "typescript": "^5.0.4"
  }
}